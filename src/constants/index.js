const ALIVE = 1;
const DEAD = 0;

const CELL_STRINGS = {
    ALIVE: '*',
    DEAD: '.',
};

const NEW_LINE_STRING = '\n';

module.exports = {
    DEAD,
    ALIVE,
    CELL_STRINGS,
    NEW_LINE_STRING
};